1、任何一个class文件都对应唯一的一个类或接口的定义信息
2、class文件是一组以8个字节为基础单位的二进制流
3、方法表集合：java语言里面无法仅仅依赖返回值的不同对已有方法进行重载，因为要重载一个方法，除了要与原方法相同的简单名称，
还需要一个与原方法不同的特征签名，在java语言层面的特征签名只包括方法名称，参数顺序和参数类型，没有方法返回值。
所以仅仅依赖返回值的话，特征签名还是一样的，判断不出来。
4、字节码的特征签名是包括方法返回值及受查异常表的。
5、attribute_name_index
6、

