1、tcp/ip 协议分层： 应用层，传输层，网络层，数据链路层
2、tcp/ip协议中还有ftp，文件传输协议和dns，域名服务
3、在传输层有两个不同的协议： tcp，传输控制协议    udp，用户数据报协议
4、发送http请求时，发送端在层与层传输数据时，从上往下，每经过一层打上一个头部信息，反之亦然。
5、三次握手：
（1） 建立连接时，客户端发送syn（同步序列编号）包到服务器，并进入到syn_send状态，等待服务器确认。
（2）服务器收到syn包进行确认，并发送一个syn+ack包，此时服务器进入syn_recv状态。
（3）客户端收到syn+ack包，向服务器发送确认包ack，此时发送完毕，都进入连接成功状态。
6、PUT方法用来传输文件，在请求报文的主体中包含文件内容，然后保存到请求url的指定的位置。
7、分块传输编码会将实体主题分成多个部分（块），每一块都用十六进制来标记块的大小，而实体主体的最后一块会使用“0（CR+LF）”来标记。
8、代理是一种有转发功能的应用程序，接收由客户端发送的请求并转发给服务器，同时也接收服务器返回的响应并转发给客户端。
9、网关：是转发其他服务器通信数据的服务器，接收客户端请求，像自己拥有资源一样对请求进行处理，其实是其他服务的数据进行转发了。
10、隧道：在相隔甚远的客户端和服务器之间进行中转，并保持通信连接的应用程序。使用SSL等进行加密，确保客户端与服务器进行安全的通信。
11、在请求中，http报文由方法、url、http版本、http首部字段等部分组成。
12、在响应中，http报文由http版本，状态码，http首部字段组成。


