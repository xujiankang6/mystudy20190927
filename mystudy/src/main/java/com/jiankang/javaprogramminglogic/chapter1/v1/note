1、 a++ 意思是先使用原来的值做其他操作后，再加

2、++a 意思是先加，再用来做其他操作。
      a前先用后加， a后，先加后用。

3、  b=a++-1    拆开为：  b = a-1    a=a+1

4、计算机系统主要使用栈来存放函数调用过程中需要的数据，包括参数，返回地址，以及函数内定义的局部变量

5、函数中的参数和函数内定义的变量，都分配在栈中，这些变量都只有在函数被调用的时候会才分配，而且在调用结束就被释放了。

6、对于数组和对象类型，它们都有两块内存，一块存放实际的内容，一块存放实际内容的地址。实际内容地址空间分配在栈上，内容分配在堆上

7、

